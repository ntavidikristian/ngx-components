import { AnimateDiff } from './animate-diff';

describe('AnimateDiff', () => {
  it('should create an instance', () => {
    const directive = new AnimateDiff();
    expect(directive).toBeTruthy();
  });
});
